# テスト画像連結の調査メモ

RMSE差分を見比べやすくするため、Godot画像・DOM画像・差分画像を一枚へ連結しよう。

ImageMagickの`montage`は、複数画像を格子状へ並べる機能だよ。ラベルは任意で、`-label`や`-font`が文字描画に関係する。実行環境に既定フォントがなく文字描画が失敗したため、比較画像ではラベルを描かず、`+append`と`-append`で画素を連結する。

- 公式資料: https://imagemagick.org/montage/
