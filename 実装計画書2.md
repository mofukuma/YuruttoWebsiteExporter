# gdweb 全機能実装計画書2

Godot 4.7.1の2D作品を、HTML、CSS、Canvas 2D、限定Wasmだけで公開する実装計画。
全完了条件へ到達するまで、独立して進められる作業を継続する。

## 1. 完了の定義

次の全項目が同じsource、同じruntime hash、同じ正常試験群で合格した状態を完成とする。

- 専用`gdweb`書き出し器からHTML、JS、Wasm、font、PCKを生成
- GPU系API、symbol、class、loader、context要求0件
- GUI登録78件の分類、実装、正常試験に空欄0件
- 2D母集団94型の分類に空欄0件
- 採用58型の実装不足0件、未実証0件
- Canvas命令10種を実装7種、構造非採用3種として全件検査
- Theme 3種×viewport 3種を実行時切替
- 日本語、IME、選択、focus、Tab、ARIA、SEOを確認
- N01〜N19、I01〜I03、警告、構造エラーを最新runtimeで合格
- Canvas MAE `< 0.006`、DOM矩形差 `< 1 CSS px`
- ERROR、crash、timeout、残留process 0件
- full Web比の容量、cold/warm median、p95、転送量を測定
- 最終レビューの有効な指摘0件

母集団抽出や静的分類だけでは完成扱いにしない。実装入口だけでも完成扱いにしない。

## 2. 固定する設計

### 2.1 状態の正本

- SceneTree、Node、Resource、signal、Control配置、Window状態、2D物理、NavigationはGodot所有
- Canvas 2Dは描画命令の実行だけを所有
- DOMは文字、入力、選択、focus、Tab、ARIA、SEOだけを所有
- Browser計算値をGodot配置へ還流させない
- DOMのFlexbox、Grid、通常flowで配置しない

### 2.2 描画境界

- GUI chrome、StyleBox、Sprite、図形、画像、clip、z、SubViewportはCanvas所有
- Label、Button文字、Link、入力値、list、tree、menu、tab、dialogの意味はDOM所有
- DOM所有glyphだけCanvasから抑止
- 同じ背景、枠、影、文字をCanvasとDOMへ二重描画しない

### 2.3 Web境界

- `canvas.getContext("2d")`以外を要求しない
- WebGL、WebGL2、WebGPU、OpenGL、GLES、Shaderを生成、読込、実行しない
- Browser内部の合成方法は制御対象外
- アプリ側のGPU API要求0件を保証

## 3. 現在の確認基準

| 対象 | 基準値 |
|---|---:|
| Godot | `4.7.1-stable` |
| Emscripten | `4.0.11` |
| GUI母集団 | 78件 |
| GUI具象 | 64件 |
| GUI所有・抽象 | 14件 |
| 2D母集団 | 94型 |
| 公開property | 3,053件 |
| property同値類 | 464群 |
| 採用候補 | 58型 |
| 構造非採用 | 33型 |
| deprecated非採用 | 3型 |
| Canvas命令 | 実装7種、非採用3種 |
| Wasm | 17,400,018 B |
| JavaScript | 209,873 B |

この表は入力固定と退行検査の基準。機能合格数ではない。

## 4. 実施順序

### Gate B GUIとDOM

#### B1 stable handleとdirty同期

1. ControlとWindowを一つの双方向handle台帳へ収録
2. 正整数handle、root sentinel `-1`、wrap、解放を固定
3. Control通知から追加、変更、削除、reparentをdirty集合へ記録
4. Window通知からvisible、position、size、popup、modal、closeを記録
5. 親を先、子を後へ並べた一括命令列をWasm境界へ送信
6. 静止0.5秒のSceneTree全走査、Wasm呼出、DOM mutationを0件化

合格条件。

- 追加、削除、再生成後もhandle重複0件
- ControlとWindowの親子関係一致
- PopupMenuの開閉、外側操作、focus復元一致
- 静止0.5秒のDOM mutation 0件

#### B2 座標、clip、z、viewport

1. `Transform2D`の6値を親相対CSS matrixへ同期
2. `transform-origin:0 0`を固定
3. pivot、17度回転、scale `0.5`、`2.0`、負scaleを比較
4. `clip_contents`を回転対応clipへ同期
5. Godot描画順とz値からDOM順、`z-index`を確定
6. rootだけでGodot pxとCSS pxを変換
7. 3 viewportでscreen寸法、入力座標、resizeを比較

合格条件。

- DOM矩形の各辺差`1 CSS px`未満
- 親子合成後の座標差`0.01 px`以下
- clip外のhit 0件
- 負scaleと回転下の入力先一致

#### B3 GUI意味変換

GUI具象64型をmanifest駆動で生成。所有・抽象14型は所有Controlで確認。

- native要素: Button、LinkButton、LineEdit、TextEdit、CodeEdit、range、progress
- ARIA要素: checkbox、switch、listbox、tree、tablist、menubar、dialog
- wrapper要素: Container、Panel、ColorRect、Texture系、分割、Graph系
- Window要素: Popup、PopupPanel、AcceptDialog、ConfirmationDialog、FileDialog、PopupMenu
- canvas-only leaf: 意味を持たない画像、装飾、separator

全Controlにwrapperを作る。意味を持たない親wrapperは`role=presentation`相当。子のSEO情報を隠さない。

#### B4 入力とIME

1. pointer、keyboard、focus、range、scrollをstable handle付き整数列へ変換
2. Button系は本家GUI入力経路へ送信。signal直呼びなし
3. LineEdit、TextEdit、CodeEditの文字、caret、selectionを同期
4. UTF-16 indexとGodot code point indexを相互変換
5. composition開始、更新、確定、取消を実装
6. composition確定前の`text_changed`を0回化
7. copy、cut、paste、undo、redo、passwordを比較
8. Tab、Shift+Tab、focus neighbor、非表示、disabledを比較
9. wheel、touch、range、menu、tree、tab固有操作を比較

合格条件。

- native版とsignal回数、順序、値が完全一致
- 日本語、絵文字、結合文字、RTLの選択範囲一致
- focus中のDOM identity維持
- disabled操作signal 0回

#### B5 Theme

同じGUI treeへ3 Themeを実行時適用。3 viewportと直積で確認。

- font、size、color、outline、shadow
- StyleBox背景、四辺border、四隅radius、content margin、shadow
- icon、texture、状態別外観
- Container separation、margin
- normal、hover、pressed、focus、disabled
- RTL、`modulate`、`self_modulate`

表現不能値はnode path、property、処理、fallback付き警告。黙った代替表示なし。

#### B6 N16一括試験

一回のnative起動と一回のChromium起動で次を完走。

- GUI登録78件
- 64具象型
- 14所有・抽象型
- 3 Theme×3 viewport
- 全固有操作
- Popup、Dialog、FileDialog、Menu、Tree、Graph、Video
- 日本語、IME、selection、Tab、ARIA、SEO

`implementationReady=true`は全件合格後だけ設定。

### Gate C Canvas 2D

#### C1 命令転送

1. 一frameの数値命令を一つのbufferへ格納
2. WasmからJavaScriptへ一括転送
3. Item順、Command順、z順を維持
4. render targetごとに命令列を分離
5. 命令数、buffer容量、実行時間を記録

#### C2 実装7命令

| 命令 | 必須処理 |
|---|---|
| `RECT` | 色、texture、region、tile、flip、transpose、outline |
| `NINEPATCH` | stretch、tile、tile-fit、margin、center有無 |
| `POLYGON` | index、頂点色、UV、texture、三角形補間 |
| `PRIMITIVE` | 点、線、三角形、四角形、頂点色、UV |
| `TRANSFORM` | command-local変換の後続適用 |
| `CLIP_IGNORE` | clip無効化と復帰、回転親clip |
| `ANIMATION_SLICE` | begin、end、offset、wrap、境界frame |

`MESH`、`MULTIMESH`、`PARTICLES`は構造非採用。build profile、Exporter拒否、代替機能を一致させる。

#### C3 画像と寿命

1. RGBA8へ固定変換
2. ImageTexture作成、更新、解放
3. 同じ画像の再upload抑止
4. filter、tile、region、atlasを実装
5. RGB変調を一時Canvasで合成
6. alpha `0`、`1/255`、`0.5`、`1`を全opへ適用
7. 親`modulate`と子`self_modulate`を累積

合格条件。

- 作成、更新、解放のhandle数一致
- 解放後のBrowser画像残存0件
- RGBA probeの各channel差`1/255`以下

#### C4 SubViewport

1. render targetへ固有OffscreenCanvasを割当
2. root、兄弟、入れ子SubViewportを分離描画
3. 透明背景、resize、破棄、親Canvas合成を実装
4. render target textureのversionで再合成を制御
5. N17の正規化MAEを`0.006`未満へ収束

#### C5 glyph所有

1. DOM所有文字のCanvas glyphだけを抑止
2. 内部Controlとcanvas-only文字はCanvasへ描画
3. shadow、outline、glyphの所有先を一意化
4. SEO本文を初期HTMLへ収録
5. hydration前後のDOM identityを維持

#### C6 N19一括試験

- 実装7命令を一画面で生成
- 命令順、画素probe、画像差、texture handle数を比較
- Canvas contextは`2d`だけ
- JavaScript、Wasm import、実行時hookのGPU入口0件

### Gate D 2D全機能

#### D1 manifest確定

`feature_manifest.json`を登録sourceとClassDBから生成。

- 94型
- 3,053 property
- 464同値類
- 採用58型
- 所有5型
- 構造非採用33型
- deprecated非採用3型

Exporter許可を実装済みの根拠にしない。実装、正常試験、所有先の三条件で判定。

#### D2 N18-A〜I

| ID | 実装対象 |
|---|---|
| N18-A | Node2D変換、visible、z、y-sort、Camera、RemoteTransform、notifier |
| N18-B | Sprite、AnimatedSprite、Line、Polygon、flip、region、負scale |
| N18-C | CPU採用機能、構造非採用機能のExporter境界 |
| N18-D | Body、Character、Area、全Shape、Cast、Joint、接触終端 |
| N18-E | TileSet、TileMapLayer、Parallax、境界tile |
| N18-F | Skeleton、Bone、modificationの採否確定 |
| N18-G | Navigation region、agent、obstacle、link、経路終端 |
| N18-H | AudioStreamPlayer2D、Path、PathFollow、Curve |
| N18-I | Theora開始、中間、終了、Canvas frame表示 |

各採用型へ非既定値と境界値を割当。型、property同値類、試験IDの空欄0件。

#### D3 状態比較

- 標準Godot 4.7.1とgdwebへ同じscene、script、Resourceを入力
- transform、速度、接触、signal、Navigation path、音声状態、動画時刻をJSON化
- 公開状態を完全一致
- 幾何差`0.01 px`以下
- 最初の不一致一件へ証拠を集約

### Gate E 資産、回帰、結合

#### E1 `.env`

- rootの`.env`を値非表示で読み込む
- secretをlog、結果JSON、HTML、PCKへ書かない
- `YNOTE_OUTPUT_DIR`などのpathは実在だけ確認
- 外部資産pathが未配置でも、他の実装と合成fixtureを継続
- 実在作品が必要な結合試験だけを待機項目として分離

#### E2 資産

- 実在作品画像の元path、元hash、変換方法、変換後hashを固定
- 音声は固定PCMからWAV生成
- 初期PCK、遅延PCK、共有Resource、到達不能Resourceを分離
- cold、同時要求、warm cacheを一回で確認
- warm再訪の不要転送0 B
- 二重mount、二重instance、UID分裂0件

#### E3 回帰

N01〜N19を最新runtime hashで一括実行。

- native Godot一回起動
- Chromium一回起動
- scene切替で全画面を実行
- 画面ごとの条件待ちは0.5秒以内
- state、画像、signal、console、processを一つのJSONへ集約

#### E4 結合

- I01 James Turrell
- I02 Lucas Pope
- I03 Daito Manabe

三作品とも状態、画像、操作、音、遅延PCK、cache、resize、Theme、正常終了を比較。

### Gate F 配布値と最終判定

#### F1 容量

- Wasm、JS、font、worklet、初期PCK、遅延PCK
- raw、gzip `-9 -n`、Brotli `q11`
- GDScript、GUI、物理、Navigation、音声、動画、TextServerの差分
- 全分類の合計を100%へ一致

#### F2 初期表示

- full Webとgdwebを同一作品、同一Chromium、同一回線条件で比較
- cold、warm各7回
- HTML FCP、runtime初期化、初回描画、操作可能時点、転送量
- median、p95、短縮率、削減率

#### F3 警告と構造エラー

正常試験完走後に実行。

1. 対応不能値の警告と固定fallback
2. 構造非採用型
3. Shader、3D、GDExtension、RenderingDevice、動的load

node path、property、処理、fallbackを全件確認。

#### F4 最終レビュー

大量変更後に一回実施。

- 設計思想の破綻
- シンプル化、冗長排除
- 速度
- 開発ルール準拠

指摘には定量値または公式調査結果を添付。設計境界を変える指摘は採用しない。

## 5. 試験構成

### 5.1 高速検査

5秒以内。

- path、hash、overlay、patch
- runtime import、class、GPU語
- DOM handle、dirty集合、Window台帳
- GUI 78件、2D 94型、Canvas 10命令
- fixtureとmanifestの空欄

入力hash不変なら再実行しない。

### 5.2 重い検査

GodotとChromiumの起動だけ5秒超を許可。対象を一回の起動へ集約。

```text
static
  ↓
native一回起動
  ↓
gdweb export一回
  ↓
Chromium一回起動
  ↓
N16 → N17 → N19 → N18 → N01〜N15 → I01〜I03
  ↓
warning → structural error
```

最初の失敗で停止。原因、入力hash、runtime hash、試験ID、証拠pathを一件へ集約して修正。

## 6. 成果物

| 対象 | path |
|---|---|
| 固定source定義 | `build/source.lock` |
| 再現overlay | `build/overlay/` |
| 本家差分 | `build/patches/web_gdweb_2d.patch` |
| native Editor build | `build/build_editor.sh` |
| runtime build | `build/build_runtime.sh` |
| GUI試験 | `tmp/gdweb/normal-matrix/n16_gui_components/` |
| SubViewport試験 | `tmp/gdweb/normal-matrix/n17_subviewport/` |
| 2D試験 | `tmp/gdweb/normal-matrix/n18_2d_features/` |
| Canvas試験 | `tmp/gdweb/normal-matrix/n19_canvas_commands/` |
| 結合試験 | `tmp/gdweb/normal-matrix/i01_*`〜`i03_*` |
| 一括結果 | `tmp/gdweb/final-gate/result.json` |
| 定量値 | `ログ/調査メモ_配布値の件.md` |

## 7. 継続規則

- 全完了条件を満たすまで、途中成果だけで最終回答しない
- 一つの機能を実装、正常試験、証拠固定まで一括処理
- 一件の失敗へ細切れ試行を重ねない
- 同じ失敗2回で公式資料、鉄板実装、既存例を調査
- blockerがあっても独立作業を先へ進める
- ユーザー入力だけで解ける最後のblockerになった場合だけ質問
- 進行中は60秒以内に短い状況を共有
- 「分類済み」「入口あり」「部分合格」を「完成」と表現しない

## 8. 参考作品のWeb実装

### 8.1 正本

保存済み参考サイトのdesktop、mobile、DOM、画像だけを使用。別の設計資料は使用しない。

### 8.2 Godot画面

- 1440 pxは左右24 px、760 px以下は左右12 px
- 固定ヘッダー、方眼背景、概要、統計、特集、検索、公開状態、案件カード
- 画面幅変更時に一括再配置
- SceneTreeを表示と操作の正本に統一
- ノード名は英語

### 8.3 gdweb出力

- Canvasへ背景、境界、画像
- 意味DOMへ文字、入力、選択、ボタン
- 初期HTMLへ主見出し、概要、作品索引を収録
- Wasm完了前は完成画面相当のHTMLを表示し、Godot進捗画面は非表示
- `Window.title`は本文へ描かず`aria-label`へ保持
- 画像は長辺960 px以下のWebP、合計170 KiB以下
- 書き出し先は`tmp/daito-site/out/`。PCKとsource treeから除外

### 8.4 一括試験

- 1440×960と390×844
- deviceScaleFactor 2でCanvas、DOM root、可視子要素の右端が親幅以内
- 同一ページで1440→390→1440の2遷移
- 主見出し位置
- 検索1件
- 限定公開2件
- 特集への本文移動
- 特集画像領域の色差画素1,000標本超過後にスクリーンショット保存
- 初期HTMLのSEO本文
- Wasm応答700 ms遅延中の初期HTML表示とGodot進捗画面非表示
- console error 0件
- 確認画像は`tmp/daito-site/`
- 同一作品を通常Godot Webへも書き出し、coldとwarmを各7回比較
- 初回Canvas表示、操作可能、転送量は通常Godot Web未満

## 9. 最終報告

次だけを最終報告へ記載。

- 完了条件の全件合格
- runtimeと全成果物のhash
- GUI、2D、Canvasの件数
- N01〜N19、I01〜I03の合否
- 容量、速度、転送量
- 警告、構造エラー、review結果
- 再現手順

未達が一件でもある場合は最終報告を行わず、実装を継続する。
