# gdweb 実装計画書

Godot 4.7.1の2D作品を、HTML、CSS、Canvas 2D、限定Wasmだけで公開するための計画。
Godotの場面・状態・配置を正本とし、ブラウザーの文字・入力・キャッシュ能力を活用する構成。

本書では「実装経路あり」「正常試験合格」「未実証」を分離。全件合格まで完成や100%達成と表現しない。

## 1. 製品契約

- Godot Editorから専用の`gdweb`書き出しを実行
- GDScriptは本家Parser、Analyzer、Compiler、VMをWasmへ含めて実行
- SceneTree、Node、Resource、signal、Control配置、2D物理はGodotを正本化
- GUI外観、画像、図形、clip、z順、ThemeをCanvas 2Dへ描画
- SEO文字、選択、入力、IME、focus、Tab、意味情報をDOMへ重畳
- HTMLをWasmより先に表示し、重要文章を初期HTMLへ収録
- 初期画面に不要な場面と資産を遅延PCKへ分離
- 共通runtimeを作品間で共有し、内容hash付きURLで長期cache
- WebGL、WebGL2、OpenGL、GLES、WebGPU、Shaderを生成・読込・実行しない
- 3D、GDExtension、C#、ネイティブプラグインを採用しない
- ブラウザー内部の描画方法は制御対象外。アプリがGPU系APIを要求しないことを保証

未対応の型、Resource、GDScript APIは書き出しエラー。採用型の未対応プロパティ値はnode path、property、処理、固定fallbackを警告へ記録。黙った代替表示なし。

## 2. 責務

| 主体 | 正本とする情報 | 実装責務 |
|---|---|---|
| Godot core | SceneTree、時刻、signal、入力状態、Control配置、2D物理・Navigation | native版とWeb版で同じ状態を生成 |
| gdweb書き出し器 | 到達依存、許可型、警告、初期HTML、PCK分割 | 対応範囲外を実行前に停止 |
| Canvas 2D | GUI外観、Theme、Sprite、図形、画像、clip、z、SubViewport合成 | Godotの描画列を順番どおり描画 |
| DOM・CSS | 文字、選択、入力、IME、focus、Tab、ARIA、SEO | 透明な意味・入力面としてCanvasへ重畳 |
| ブラウザー | 文字raster、画面外paint省略、HTTP cache、音声出力 | 標準機能を利用。独自の画面外判定を重ねない |
| 限定Wasm | 本家GDScript VM、場面更新、2D server、差分生成 | 作品ごとに再buildしない共通runtime |

配置はGodotだけが決定。DOMのFlexbox、Grid、通常flow、intrinsic layoutで位置を決めない。CSSはGodotが計算した親相対`Transform2D`、寸法、clip、z、文字方向の反映に限定。

## 3. ディレクトリ

```text
gdweb/
├─ build/                  再現可能な限定runtime定義
├─ tests/                  棚卸し、静的検査、正常試験ゲート
├─ ログ/                   調査結果と定量値
├─ tmp/                    Godot source、fixture、画像、実測証拠
├─ LINESeedJP_A_OTF_Rg.otf Godot文字計量用
├─ LINESeedJP_A_OTF_Rg.woff2 DOM文字表示用
└─ 実装計画書.md
```

`tmp/`は生成可能な作業領域。改変Godot sourceは`tmp/godot-source`、固定Emscriptenは`tmp/emsdk`、配布候補は`tmp/gdweb/runtime-proof`へ配置。

### 移設成立条件

現在の移動先にはビルド定義、検査コード、調査ログ、フォントが存在。改変Godot source、正常画面fixture、最新実測証拠は移動先へ未同期。スクリプトにも移動前の`.tmp/`と`gdweb/`入れ子前提が残る。

実装再開前に次を一括実施。

1. Godot 4.7.1改変sourceを`tmp/godot-source`へ同期
2. Emscripten 4.0.11を`tmp/emsdk`へ固定
3. 正常画面fixtureと比較証拠を`tmp/gdweb`へ移設
4. `build/`と`tests/`のroot解決を現在のディレクトリへ統一
5. `.tmp/`参照と`gdweb/`二重参照をゼロ化
6. font、profile、source、成果物の絶対pathを静的検査
7. 同じ入力から同じhashのEditor、JS、Wasmを生成

このゲートを通るまで、旧作業領域の成果物を移動先の再現可能な最終成果物と扱わない。

## 4. 現在の実装状態

### 4.1 限定runtime

`build/build_runtime.sh`と`build/gdweb.build`に次の構成あり。

- `gdweb_2d=yes`
- `opengl3=no`、`vulkan=no`、`threads=no`、`javascript_eval=no`
- `disable_3d=yes`、`disable_advanced_gui=no`
- 2D物理とNavigation2Dを有効化
- GDScript、TextServerFallback、FreeType、MSDFGen、SVG、WebPを有効化
- Ogg、Vorbis、Theora、Web Audioを有効化
- Shader、Material、RenderingDevice、GPU粒子、CanvasGroup、MeshInstance2D、MultiMeshInstance2Dをprofileで除外
- LINE Seed JPのWOFF2をruntimeへ同梱

旧作業領域の現在候補はWasm `21,928,910 B`、JS `230,738 B`、WOFF2 `2,105,116 B`。

| 成果物 | SHA-256 |
|---|---|
| Wasm | `a9b57a3a9fcc9d693ed4869d803e79066ba0015637440d192ef72803ec148700` |
| JavaScript | `de1137b2ca317a8954d07a4a04f643dccdb43024f4a59567785b1038e77898a2` |
| 日本語OTF | `33ff0df129c7b73d24e42b42dc235e51dfbb578163e08503b839c4235872f6f1` |
| 日本語WOFF2 | `6d46d695a3258c63b267a87dddfb792f5a3995a04afce0643276256b344b76ce` |

移設後の再build前の参考値。機能確定後に容量、圧縮後容量、起動時間を再測定。

### 4.2 書き出し器

実装経路あり。

- 専用`EditorExportPlatform`
- 静的HTML、JS、Wasm、font、初期PCKの出力
- 選択場面から到達するResource、autoload、`preload`依存の収集
- hash付き遅延PCKとcold/warm cache
- 型、Resource、GDScript APIの固定許可表
- 警告へnode path、property、処理、fallbackを記録
- 同一入力の再書き出しhash一致

正常経路の完成条件は、全採用機能を含む作品で連続二回の終了code 0、通常警告0、成果物hash一致。構造エラー試験は全正常画面と警告画面の合格後だけ実行。

### 4.3 Canvas 2D

Godot描画命令10種をsourceから抽出。静的分類は実装7種、構造非採用3種、未分類0。

| 分類 | 命令 |
|---|---|
| 実装経路あり | `RECT`、`NINEPATCH`、`POLYGON`、`PRIMITIVE`、`TRANSFORM`、`CLIP_IGNORE`、`ANIMATION_SLICE` |
| 現在非採用 | `MESH`、`MULTIMESH`、`PARTICLES` |

実装済み経路。

- 矩形、線、円、多角形、頂点色、UV、texture region、tile
- NinePatchのstretch、tile、tile-fit
- 親`modulate`と子`self_modulate`の累積
- alpha `0`、`1/255`、`0.5`、`1`、RGB変調
- ImageTextureの更新・解放・version管理
- SubViewportごとのOffscreenCanvasと親Canvasへの合成
- render targetの透明合成、resize、破棄
- glyph atlasのうちDOM所有文字だけを抑止
- 一フレームの命令をWasmからJavaScriptへ一括転送

静的到達確認は正常描画の証明ではない。N17のSubViewport比較、N19の全7命令比較、画像差、色probe、破棄後handle数まで最終runtimeで再実証。

現行`tests/runtime_static.cjs`はWasm importとClassDBを主な合否条件とし、JSのGPU語を記録するだけの経路が残る。移設ゲートで次を`ok`の必須条件へ追加。

- JSの`getContext("webgl")`、`webgl2`、`webgpu`、OpenGL・GLES入口0件
- Navigation2D、Theora、2D物理、音声の必須classあり
- 記録項目と合否項目の差0件

### 4.4 GUIとDOM

Godot 4.7.1のGUI登録78件を母集団化。内訳は具象64件、Control 58件、Window 6件、所有・抽象14件。全件を型名の手書き完了表ではなく、登録source、ClassDB、manifest、試験IDで照合。

現在の実装方針。

- 全Controlへ親相対の透明wrapper
- canvas-only親のwrapperは`role=presentation`相当。子のSEO情報を隠さない
- Videoのような意味を持たないleafだけ`aria-hidden`
- Canvas本体は`aria-hidden=true`
- semantic DOMはLabel、Button、Link、入力、選択肢、list、tree、menu、tab、dialogなど
- stableな正整数handleをC++が払い出し
- 初期HTMLと実行中DOMの要素identityを維持
- 追加、削除、reparent、Window状態をdirty集合で同期
- 静止frameのSceneTree全走査と同値DOM更新をゼロ化
- DOMはGUI背景、枠、角丸、StyleBox影、画像を描かない
- GUI chromeと文字以外のThemeはCanvas 2Dだけが描画
- ScrollContainerはGodot scroll値を正本化。wheel、touch、focus移動をGodotへ戻す

動作確認がある範囲。

- 親子移動、17度回転、`0.5`・`2.0` scale、負scale
- z、clip、親外の子、安定handle、削除・再生成
- Button、CheckBox、CheckButton、LinkButton、LineEdit、ProgressBar、H/VSlider
- Tab、Enter、Space、矢印、selection、undo、composition確定
- 12 ContainerのGodot計算済み矩形
- RTL、font size、font color、日本語、絵文字、結合文字
- 3段階viewport resize

全78件の正常試験は未完。全GUI画面N16は、3 viewport×3 Themeの途中まで到達。現在の先頭停止点は`PopupMenu.visible`のnative/Web不一致。Popup、Dialog、FileDialog、Video、複合Control、全固有操作、Theme動的切替の9組合せを完走していない。

### 4.5 2D機能

Godot登録sourceとTheora moduleから94型を抽出済み。移設前の最終候補は公開property 3,053件、非既定値同値類464群。移設先の調査ログには一段前の3,048件、463群が残るため、再生成後のJSONだけを正本化。

| 現在分類 | 型数 | 意味 |
|---|---:|---|
| 採用候補 | 58 | 移設前の最終候補。2D限定版で実装・正常試験する型 |
| 実装入口あり | 6 | 移設前候補の内訳。`Node2D`、`Sprite2D`、`StaticBody2D`、`RigidBody2D`、`CollisionShape2D`、`RectangleShape2D` |
| 派生先で所有 | 5 | 移設前候補の内訳。抽象・内部型を具象型の試験で確認 |
| 実装不足 | 47 | 移設前候補の内訳。Exporter、server、Canvas、試験のいずれかが不足 |
| 構造非採用 | 33 | Shader・GPU・Mesh経路など限定版契約外 |
| deprecated非採用 | 3 | `deprecated=no`で除外 |

94型の母集団照合だけ合格。採用候補58型はすべて最終正常試験が未完。移設後にClassDBと分類を再生成し、58型、6型、5型、47型の件数を全件assertしてから確定。特に次を未実装扱いで進める。

- `CharacterBody2D`、`AnimatableBody2D`、`Area2D`
- 全Shape2D、cast、joint、接触終端
- `AnimatedSprite2D`、`Line2D`、`Polygon2D`
- Camera2D、RemoteTransform2D、visible notifier
- TileSet、TileMapLayer、Parallax2D
- Skeleton2D、Bone2D、modification
- NavigationRegion2D、Agent、Obstacle、Link、経路終端
- AudioStreamPlayer2D、Path2D、PathFollow2D、Curve2D
- VideoStreamTheoraの開始・中間・終了

GPU固有機能をWebGLで復活させない。採用する場合はGodot CPU状態とCanvas 2D命令へ置換し、native版との意味境界を明記。意味を保てない型は構造非採用へ分類し、理由と代替手段を固定。

## 5. DOM・CSSの実装仕様

### 5.1 座標

```text
viewport root
└─ Control wrapper
   position:absolute
   left:0; top:0
   width/height = Godot local size
   transform = Godot parent-relative Transform2D
   transform-origin:0 0
```

- 親子DOMをGodotのControl親子へ対応
- rootだけでGodot pxとCSS pxを変換
- anchor、offset、grow、Container、RTLの配置結果はGodot値を使用
- screenとviewportの実寸、device scale、resize後の入力座標を相互検査
- `getBoundingClientRect()`は試験だけに使い、実行中配置の入力にしない
- Godotのz順を`z-index`とDOM順へ変換
- SubViewport内Controlはviewport-localからroot座標へ合成

### 5.2 文字

- Godot文字計量は`LINESeedJP_A_OTF_Rg.otf`
- DOM表示は`LINESeedJP_A_OTF_Rg.woff2`
- 日本語、ASCII、絵文字、結合文字、改行、wrap、ellipsis、RTLを試験
- Label、見出し、説明、Linkを初期HTMLへ出力
- font読込前後でDOM identity、selection、focusを維持
- DOM所有文字のCanvas glyphだけを抑止
- 内部ControlやCanvas所有文字はglyph atlasを維持
- SEO本文を`canvas`、`aria-label`だけへ閉じ込めない

文字の所有を次のように固定。

- glyph、font shadow、font outlineはDOM所有
- `text-shadow`とChromiumの文字strokeで表現可能な値だけDOMへ同期
- 表現不能な複数影、outline合成、特殊blendは固定fallback警告
- StyleBox、Panel、Button chromeのshadowはCanvas所有
- 同じ影をDOMとCanvasの両方へ描かない

文字rasterとCanvasの完全一致はDOM制約として例外を許す。ただし文字位置、矩形、内容、選択範囲、改行、z、clip、意味情報は完全一致対象。

### 5.3 入力と操作

- DOM eventをstable handle付き整数列へ変換
- 本家`BaseButton`状態機械を経由。signal直呼びなし
- pointer、keyboard、focus、Tab、IME、selection、clipboard、undoをGodotへ反映
- disabledはGodotのfocus契約を保ち、操作signalだけ抑止
- UnicodeのUTF-16 indexとGodot code point indexを相互変換
- composition確定前はtext signalを抑止
- native Tab順を使用。CSSの自動Tab順へ任せない
- scroll、range、menu、tree、tab、dialogの固有操作を各一回のevent列で比較

### 5.4 Theme

同じGUI treeへ3 Themeを実行時に切替。各Themeを3 viewportで確認。

- font、size、color、outline、shadow
- StyleBoxの背景、四辺border、四隅radius、content margin、shadow
- icon、texture、状態別外観
- Container separationとmargin
- normal、hover、pressed、focus、disabled
- RTLと文字方向
- `modulate`、`self_modulate`、alpha 0、半透明、RGB変調

Canvas画像を正本とし、DOM computed styleは文字・入力・透明性だけを確認。Theme切替後に古いCanvas命令、DOM style、texture cacheを残さない。

## 6. 主機能単位の実装

### 6.1 再現build

1. 移設pathの統一
2. Godot commit、Emscripten、SCons引数、profileの固定
3. native EditorとWeb templateを各一回build
4. runtime静的検査
5. hash固定後はsourceまたはbuild入力が変わるまで再buildしない

合格条件。

- Wasm importにGPU、OpenGL、WebGL、GLES、WebGPU API 0件
- JSに`getContext("webgl")`、`webgl2`、`webgpu` 0件
- Shader、ShaderMaterial、CanvasItemMaterialのClassDB登録とloader 0件
- 必須2D、物理、Navigation、音声、動画classあり
- 通常Godot buildへgdweb専用経路が混入しない
- 上記すべてを`tests/runtime_static.cjs`の終了codeへ反映

### 6.2 GUI全件

1. N16で登録78件とClassDBを再抽出
2. 64具象型を一画面群へ配置
3. 14所有・抽象型を所有Controlの操作で確認
4. PopupMenu可視状態をGodotのWindow通知からdirty同期
5. 全固有eventと状態をnative/Webで比較
6. 3 Theme×3 viewportを一回の起動で切替
7. scroll端、はみ出しmask、anchor、position、screen寸法、Tab端を確認
8. 安定0.5秒でDOM mutation 0、SceneTree全走査0を確認

### 6.3 Canvasと画像

1. N17でroot、兄弟SubViewport、入れ子SubViewportを比較
2. N19で実装7命令を一括比較
3. Spriteの回転0/90/任意角、scale 0.5/2/負、flip、region、画面端を確認
4. `modulate`と`self_modulate`の固定6値を全描画opへ適用
5. texture作成、更新、tile、filter、render target、解放を確認
6. 画素probe、命令順、handle数、PNG差を比較

### 6.4 2D場面とserver

N18の9画面へ採用型だけを機能単位で集約。構造非採用33型とdeprecated非採用3型は正常sceneへ配置しない。

| ID | 主機能 |
|---|---|
| N18-A | Node2D変換、可視、z、y-sort、Camera、RemoteTransform |
| N18-B | Sprite、AnimatedSprite、Line、Polygon、texture、flip、負scale |
| N18-C | CPU採用する粒子、Mesh展開、Light、遮蔽、backbuffer |
| N18-D | Body、Character、Area、全Shape、Cast、Joint、Touch、終端 |
| N18-E | TileSet、TileMapLayer、Parallax2D、境界tile |
| N18-F | Skeleton2D、Bone2D、modification |
| N18-G | Navigation region、agent、obstacle、link、経路終端 |
| N18-H | AudioStreamPlayer2D、Path、PathFollow、Curve |
| N18-I | Theora開始・中間・終了、frameのCanvas表示 |

各採用型で既定値だけを置かない。採用型に属する同値類へ非既定値と境界値を重複なく割当。型、property、同値類、正常試験IDの対応に空欄を許さない。

非採用36型は型、理由、build条件、Exporter拒否、代替機能を静的manifestへ収録。構造エラーの実行確認は全正常・警告試験の後に一括実行。

### 6.5 資産と遅延読込

- 画像は`参考サイト/`の実在作品を使用
- 元path、元hash、変換方法、変換後hashを固定
- 生成画像を最終fixtureへ使わない
- 音は固定PCMからWAVまたはAudioStreamWAVを生成
- 初期PCK、遅延PCK、共有Resource、到達不能Resourceを分離
- cold、同時要求、warm cacheを一回の試験で確認
- warm再訪時の不必要な転送0 B
- 二重mount、二重instance、同じUIDの分裂なし

## 7. 正常試験

### 7.1 判定

- 標準Godot 4.7.1とgdwebへ同じscene、script、Resourceを入力
- 公開状態JSONを完全一致
- 幾何位置の絶対差`0.01 px`以下
- DOM矩形の各辺差`1 CSS px`未満
- RGBA probeの各channel差`1/255`以下
- PNG正規化MAE `< 0.006`
- 文字内容、selection、Tab順、ARIA、signal回数を完全一致
- Canvas contextは`2d`だけ
- 終了code 0、signal 0、timeout 0
- Godot `ERROR`、crash、console error、page error 0件
- fixture由来のGodot、Chromium、Node、HTTP process残留0件

DOM文字rasterだけはMAE未達を許容可能。Canvasだけの差分画像とDOM文字mask後の差分画像を併記し、Canvas、Sprite、Theme、clip、位置の不一致を文字差へ混ぜない。

### 7.2 既存正常画面の扱い

旧候補で合格値あり。

- N01/N02 Canvas基本・texture
- N03/N04 作品画像の回転・拡縮・負scale・端
- N05/N06 GUI回転・拡縮
- N07/N08 z・clip・親子変換
- N10 Container
- N11 2D物理
- N12 音声
- N14 正常終了
- N15 遅延PCK
- N19 Canvas命令
- I01、I03 結合作品

最終基準`0.006`を超える既存値は未合格として再実証。

- N09 interactive GUI `0.00951369`
- N13 resizeの一部 `0.00949`、`0.00723`
- N17 SubViewport `0.00678051`
- I02 Lucas Pope `0.00941634`
- warning fallback `0.00966885`

最新hash、LINE Seed JP、全GUI、全2Dの実装後に全正常画面を一括実行。古いhashの合格値だけで最終合格にしない。

### 7.3 結合試験

`参考サイト/`から三作品を選び、同じGodot機能で再構成。

| ID | 参照 | 必須接続 |
|---|---|---|
| I01 | James Turrell | Sprite、Canvas光面、DOM文字、clip、z、resize、Theme |
| I02 | Lucas Pope | 作品一覧、Container、入力GUI、Tab、音、画像、Theme |
| I03 | Daito Manabe | 黒いindex、細線、Sprite、物理、遅延PCK、cache |

三作品とも最新runtimeで標準Godot/Web比較、全操作、正常終了、残留0を満たすこと。

## 8. 試験運用

- `tests/`の一つの正常ゲートから静的検査、native、export、Chromiumを順番に実行
- 画面や主機能を完成させてから一括実行
- 最初の正常失敗で停止。原因と証拠を一件へ集約して修正
- 同じ失敗を細切れの試験で反復しない
- 画面内の成立待ちはeventまたは条件待ち。上限0.5秒
- Godot、Wasm、Chromiumの起動が5秒を超える場合、同じ起動で対象画面を全件実行
- 入力hashとruntime hashが同じ単体試験を再実行しない
- screenshot、state、log、結果JSONは`tmp/gdweb/<試験ID>/`
- 正常試験と警告試験を完走するまで構造エラーfixtureを実行しない

現行`tests/normal_gate.cjs`は44 commandを逐次起動し、N16、N18、I01も全件収録していない。timeout上限の合計も2,230秒。R11の一括・短時間試験を満たさないため置換対象。

置換後の正常ゲート。

- 入力hash単位でimportを一回
- runtime hash単位でexportを一回
- native Godotを一回起動し、試験sceneを順番に切替
- Chromiumを一回起動し、同じscene群を順番に切替
- 画面ごとの成立待ちは0.5秒。GodotとChromiumの起動だけ個別上限を許可
- 全画面の状態、画像、操作結果を一つの結果JSONへ集約
- failure時は最初の画面、判定、証拠pathを返して終了

試験順。

1. path・font・source・profileの静的検査
2. runtimeのGPU不在検査
3. N16 GUI全件
4. N17 SubViewport
5. N19 Canvas全命令
6. N18 2D全機能
7. N01〜N15の回帰一括
8. I01〜I03の結合一括
9. 対応不能値の警告とfallback画面
10. 構造非採用の書き出しエラー

## 9. 定量化

機能凍結後、同一回線条件、同一Chromium、同一作品、同一圧縮設定で本家full Web出力と比較。

### 容量

- Wasm raw、gzip `-9 -n`、Brotli `q11`
- JS、font、worklet、初期PCK、遅延PCK
- GDScript、GUI、2D物理、Navigation、音声、動画、TextServerの独立差分
- 独立差分できない機能はlink前object比と配布差分を分離
- 全機能分類の合計を100%へ一致

### 初期表示

- cold、warm各7回
- HTML FCP
- runtime初期化
- Godot初回描画
- 操作可能時点
- 総転送量
- medianとp95
- 本家full比の短縮率と転送削減率

旧hashでの速度値を最終値へ流用しない。機能凍結した一つのhashだけで再測定。

## 10. 実施順序

### Gate A 移設

- `tmp/`へsource、toolchain、fixture、証拠を集約
- 全pathを現在のrootへ統一
- native EditorとWeb templateを再現build

### Gate B GUI

- PopupMenu可視同期を修正
- GUI 78件、3 Theme、3 viewport、全固有操作を完走
- scroll、mask、anchor、screen、position、Tab、IME、日本語を合格

### Gate C Canvas

- SubViewport透明合成を`0.006`未満へ修正
- Canvas 7命令、texture、modulate、解放を完走
- SpriteとGUI chromeの二重描画0件

### Gate D 2D全機能

- 移設後に確定した採用型の実装不足を主機能単位で実装
- 94型、公開property、同値類の分類を再生成し、件数を全件assert
- N18-A〜Iを一括比較

### Gate E 回帰と結合

- N01〜N19を最新hashで一括実行
- I01〜I03を一括実行
- MAE超過、ERROR、crash、残留processを0件化

### Gate F 配布値

- 容量比を再測定
- full Webとのcold/warm速度を再測定
- 警告、最後に構造エラーを検査
- 1エージェントで設計、単純性、速度、開発ルールを最終レビュー

## 11. 完了条件

- 移動先だけでEditor、runtime、全fixtureを再現可能
- GPU系API、symbol、class、loader、context生成0件
- GDScript、2D物理、Navigation、音声、動画が採用契約どおり動作
- GUI登録78件のmanifest、実装、正常試験に空欄0件
- 2D母集団94型の分類に空欄0件
- 確定した採用型の実装不足0件、正常試験未実証0件
- Canvas命令10種の実装または構造非採用理由に空欄0件
- LINE Seed JPで日本語の計量、表示、選択、IME、SEOを確認
- Theme 3種×viewport 3種を動的切替して合格
- Sprite、Character、画像変換、modulate、clip、z、SubViewport、物理終端を合格
- 全正常画面の状態一致、Canvas MAE `<0.006`
- 正常終了、crash 0、ERROR 0、残留process 0
- I01〜I03の三作品を最新runtimeで合格
- full Web比の容量、cold/warm median・p95、転送量を定量化
- 警告fallbackと構造非採用エラーを最後に合格
- 最終レビューの有効な指摘0件

## 12. 正本

- [ChromiumとWeb描画仕様](ログ/調査メモ_ChromiumとWeb描画仕様の件.md)
- [Godot本家Web出力](ログ/調査メモ_Godot本家Web出力の件.md)
- [近縁プロジェクト](ログ/調査メモ_近縁プロジェクトの件.md)
- [限定Godotエンジン](ログ/調査メモ_限定Godotエンジンの件.md)
- [実2DWeb書き出し経路](ログ/調査メモ_実2DWeb書き出し経路の件.md)
- [GUIのDOM変換とGPU完全排除](ログ/調査メモ_GUIのDOM変換とGPU完全排除実証の件.md)
- [GUI完全性試験表](ログ/調査メモ_GUI完全性試験表の件.md)
- [Canvas命令と2D資源網羅性](ログ/調査メモ_Canvas命令と2D資源網羅性の件.md)
- [2D全機能棚卸し](ログ/調査メモ_2D全機能棚卸しの件.md)
- [全機能所有先棚卸し](ログ/調査メモ_全機能所有先棚卸しの件.md)
- [正常系機能組合せ試験](ログ/調査メモ_正常系機能組合せ試験の件.md)
- [Godot正常終了](ログ/調査メモ_Godot正常終了の件.md)
- [Wasm主機能別容量](ログ/調査メモ_Wasm主機能別容量の件.md)
- [初期表示速度比較](ログ/調査メモ_初期表示速度比較の件.md)
