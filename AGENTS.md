# 開発ルール
R01 短く喋って。
R02 変更履歴的記述を計画書やプログラムに入れないこと。
R03 後方互換不要。後方互換を確保するための未使用コードを残さないこと。
R04 シンプルで読みやすい短いコードを書くこと。似ている処理の共通化を常に考えること。識別子は短く保て。
R05 日本語コメントをいれること。コメントはまず「何をするものか」「何のための情報か」「何を判断する処理か」「何を実現したい設計か」目的優先で書き、その後に詳細を補足する形で統一。先頭にそのファイルの概要、責務、設計思想を書いて。定数や設定値の右にはコメントを入れよ。関数やクラスやの先頭、そして処理単位でコメントを入れよ。
R06 指示文の内容を成果物にいれないこと。日本語コメント、計画書、資料には、常に対となるプログラムや設計の内容から考えて書け。
R07 できるだけ専門用語を使わずにわかりやすいシンプルな説明をせよ。
R08 2回以上失敗で詰まった場合、または、実装計画を考える前には、推定だけで判断したり、試行錯誤を何度も繰り返さず、鉄板の方法、最新手法、最新技術、サンプルコード、参考モジュール、フォーラムの情報などをWeb調査して解決せよ。そのとき　ログ/調査メモ_XXXの件.md　を残して。計画書や報告書などの.mdも ログ/ジャンル名_XXX.md などへ。
R09 skills/ にスキルがある。活用せよ。作成時は汎用的な記載を行え。
R10 中間ファイルやスクリーンショット確認などは常に tmp/ 以下に生成せよ。
R11 画面、主機能単位で一気に実装を行い、一括テストせよ。１つのエラーに囚われて細切れ監査を繰り返したり、無駄に長時間waitしたり、最後に終了しないテストや、5秒以上のテストは禁止。単体テストはファイルのハッシュ値不変なら実行しないなど短縮を行え。状況の再現に時間がかかるものは、状況直前にするテストを書いて。5秒以上のテストはソフトの起動が重いものだけに限定。その場合、全テストを一気にテストできるようにせよ。テストはtests/ へまとめ。ブラウザの表示はPlaywrightを使ってテストせよ。
R12 MCPはトークンを消費する。可能な限り、エージェントがコードを書いて必要な定義だけ読み込み、データをフィルタするなどトークン削減を工夫せよ。
R13 computer useなど画面操作はトークン消費し、PC操作を不可能にする。代替可能なCLI、MCPを使うか、エージェントがコードを書いて代替せよ。代替不可能な処理のみcomputer useを用い、ルーチンテストに入れることは絶対禁止。
G01 Godotは　/Applications/Godot 4.7.1.app なければhomebrewなどでtmpローカルインストール。画面テストコードはCLIにて起動し、テスト開始即`window_set_position(10000,10000)`とし、--resolutionを半分にし、コードでスクリーンショット取得。または--write-movieで動画。データテストはヘッドレスでテスト。
G02 大量の変更をしたらレビューせよ。「設計思想の破綻チェック」「シンプル化・冗長排除」「速度最適化」「開発ルール準拠」の４つを最重視して１エージェントレビュー。レビュー指摘の際には定量的なデータ、Web調査結果のうち1つ以上を根拠、証拠として示せ。メインエージェントは見当違いかチェックせよ。設計思想が変わるような指摘は棄却。初期の設計思想に沿って問題解決すること。
G03 Godotプロジェクト内の検証やテストはtestsやtmpへいれること。後まで残したいテストコードや素材はtestsへ。
G04 ノード名は英語。日本語にしないこと。
P01 Python実行はuvを使え。
D01 READMEや説明文は「何のためにやるか」を先に書き、そのあとに手順や条件を置く。仕様の羅列、作業ルールの指示文、将来の配布方針のような経緯は書かない。文体は「〜しよう」「〜だよ」のゆるっとした話し言葉。
D02 〜だけ　は使用禁止。限定の言い切りは、書き手が確かめていない条件を隠して嘘になる(例:「置くだけ」「設定するだけ」)。
H01 誇張・営業口調は禁止。「pivotal / testament / vibrant / rich / nestled / underscore / highlight / delve / landscape / tapestry」等のAI頻出語、重要性の水増し(「〜を象徴し」「〜を反映し」等の -ing 修飾)、権威付けの名前列挙、「Despite challenges / Future Outlook」型の定型節
H02 書式の癖を消して　emダッシュ(—)・エンダッシュ(–)は禁止(著者サンプルにある場合のみ比率を合わせる)。過剰な太字、「ラベル: 説明」型リスト、見出しのタイトルケース、絵文字、カーリークォート、見出し直後の同語反復文を除去する。
H03 チャットボット残渣とフィラー禁止:「素晴らしい質問ですね」等の挨拶・追従、知識カットオフ免責と推測による穴埋め、「in order to」型の冗長句、過剰なヘッジ、空虚なポジティブ結び、「本当の問題は〜」「さあ掘り下げよう」等の演出フレーズ、誰も言っていない異議・偽の代替案への反駁を削る。
