# 改善点

Godotがそのまま描いた絵と、Webへ書き出した絵を重ねて測り、
違いの大きいところから直していった記録。数はすべて手元の実測値。

## いまの揃いぐあい

一画面ごとの食い違い(MAE)と、全画面をまとめた調和平均。

| 画面 | 中身 | MAE |
|---|---|---|
| shapes3d | 3Dのmesh五種、光、影、材質 | 0.0111% |
| mixed3d | 3Dの上にHTMLの文字を重ねる | 0.0770% |
| text | 文字だけ。日本語、五寸法、二段組 | 0.2560% |
| nodes | 文字を出すControl十七種 | 0.6368% |
| rotate | 文字を輪へ並べて回す。三重、逆回りあり | 0.5579% |

**全画面の調和平均は0.0453%。** 目標の0.05%を下回っている。
調和平均は小さいほうへ引かれるので、よく揃っている画面の効きが素直に出る。

## 直したこと

### 1. 色が全面で1ずれる

GodotのPNG保存は切り捨て、Browserは四捨五入をする。
`0.12 x 255 = 30.6` がGodotで30、Webで31になり、絵の全面に差が出ていた。

検査で使う色を8bitの境目のすぐ上へ置いて解消。1.9869% -> 1.6837%。

### 2. 書体が別物

Web側は書体の指定が無いと`sans-serif`(手元ではHelvetica)になる。
Godotは内蔵の書体を使う。**別の書体では画素は揃わない。**

同じ書体のTTFとWOFF2を両方へ渡して 1.6837% -> 0.5209%。ここが一番効いた。

### 3. 字の骨組みを格子へ寄せていた

Godotの既定は`hinting=3`で、字の骨組みを画素の格子へ寄せる。Browserは寄せない。
総当たりで測った結果。

| hinting | 0 | 1 | 2 | 3 |
|---|---|---|---|---|
| MAE | **0.6964%** | 0.7461% | 0.8694% | 0.7461% |

| subpixel_positioning | 0 | 1 | 2 | 3 |
|---|---|---|---|---|
| MAE | 0.7589% | 0.6964% | 0.6669% | **0.6528%** |

`hinting=0, subpixel_positioning=3`で 0.7461% -> 0.6528%。

### 4. 字の縁の描きかたが違う

Godotは字の縁を灰色の濃淡だけで描く。Browserの既定はそうではない。
DOMのrootへ`-webkit-font-smoothing:antialiased`を足して揃えた。

0.6528% -> 0.2560%。**六割減。** これはaddonを使う全員の見た目が良くなる直し。

### 5. 回る画面で、GodotとWebの時が合っていなかった

輪の形に並べた文字を回す画面を足したら1.4085%も出た。
調べると絵の乱れではなく、**両者が別の瞬間を写していた**。
見本を撮るときはGodotの時間を止めるが、Web側は普通に回り続けるため。

場面の中で「始まってから何秒の姿か」を外から決められるようにして、
両方を同じ瞬間で写すようにした。1.4085% -> 0.5579%。

回っても文字はHTMLのまま。55文字それぞれが別の傾きを持ち、
選んでコピーできる状態が保たれていることを検査で確かめている。

### 6. Buttonの字だけ上へ寄っていた

Controlごとに測って見つけた。ButtonとLinkButtonの系統だけが7〜11%で、
Labelは0.2%だった。

原因はGodotがButtonに行の間隔を渡さないこと。DOM側で行の高さが
文字寸法ちょうどまで縮み、Browserが字を上へ詰めていた。
書体本来の行送りを下限にして揃えた。

| Control | 前 | 後 |
|---|---|---|
| CheckBox | 11.42% | ほぼ解消 |
| MenuButton | 10.78% | 4.21% |
| CheckButton | 9.31% | ほぼ解消 |
| LinkButton | 8.73% | 2.74% |
| Button | 7.22% | ほぼ解消 |

画面全体では 1.4701% -> 0.7290%。TextEditの大きさも直して 0.6368% まで下がった。

## 効かなかった道

試して駄目だったものも書いておく。同じ道を二度通らないため。

| 試したこと | 結果 |
|---|---|
| 文字を縦へずらす(0〜1.25px) | 0が最適。ずらすと悪化 |
| `line-height`を`normal`にする | 変化なし |
| 文字のなめらかさ`antialiasing=0/2` | 1が最適 |
| 字の作り直し`oversampling=2/4` | 1.0が最適 |
| `text-rendering:optimizeLegibility` | 変化なし |
| 字を太らせる`text-stroke` | 悪化 |

「字が上下にずれている」と最初は見立てたが、これは測りかたの誤りだった。
明るさの境目で上端を探すと、縁のぼけた画素が閾値をまたいで別の行に見える。
ずらして測り直したら0が最適で、位置は元から合っていた。

## 残っている違い

### TextEditの一行目が2px上へ寄る

textareaは、中身が一行でも二行分の高さを見積もる(実測で`scrollHeight`が
一行の絵の倍の50)。この見積りの違いから、一行目の字が2pxだけ上へ出る。
LineEditとCodeEditは同じ書きかたで完全に揃うので、複数行のtextarea固有のもの。

行の高さを24px、25px、`normal`へ変えて試したが、いまの25pxが一番良かった。
nodesの画面の残り誤差の半分以上がここ。

### MenuBarはGodot側が画面へ描かない

Godotは`FILE`を画面へ描かず、Web側は描いていた。実測で明るい画素がGodot 0、Web 217。
`get_menu_count()`は1、`get_menu_title(0)`は`FILE`を返すので、
DOM側のほうが中身に忠実。絵の比較には向かないので、比べる画面からは外した。
一覧表では対応済みとして数えている。

### 字の縁のぼけかた

Godot(FreeType)とBrowser(Skia)で字を点へ落とす計算が違う。
設定では消せない。文字の画素の93%は完全一致し、残るのは縁の一画素分。

## 対応しているGodotのnode

`ClassDB`から数えた実際の数。

| 種類 | 数 | 扱い |
|---|---|---|
| Control | 60 | 下の表のとおり |
| Node2D | 47 | Godotの絵のまま出る |
| Node3D | 108 | Godotの絵のまま出る(3D版テンプレート) |
| その他 | 25 | 画面に出ないもの |

Controlの内訳。

| 扱い | 数 | 割合 |
|---|---|---|
| 文字をHTMLで出す | 18 | 30.0% |
| 文字を持たないので絵のまま | 34 | 56.7% |
| 未対応 | 8 | 13.3% |

文字を持つControlに限れば、18/26 = **69.2%** が対応済み。

未対応の8つと、その理由。

| Control | 理由 |
|---|---|
| RichTextLabel | BBCodeの一部が再現できない |
| ColorPicker | 見た目が複雑 |
| ColorPickerButton | 見た目が複雑 |
| GraphEdit | 編集器向け |
| GraphElement | 編集器向け |
| GraphFrame | 編集器向け |
| GraphNode | 編集器向け |
| VideoStreamPlayer | 動画は対象外 |

## 次にやるなら

1. TextEditのつまみを描く。nodesの残り誤差の半分以上がここ
2. MenuBarの見出しを、Godotと同じ条件で出す
3. RichTextLabelのうち、色と太さだけの簡単なBBCodeへ対応する
